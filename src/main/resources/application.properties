# Spring DataSource
spring.datasource.url=jdbc:postgresql://ep-wild-silence-a96cpr66-pooler.gwc.azure.neon.tech/bazyjava?sslmode=require
spring.datasource.username=bazyjava_owner
spring.datasource.password=npg_5YzTPkaKCv2b
spring.datasource.driver-class-name=org.postgresql.Driver
# Spring JPA
# The SQL dialect makes Hibernate generate better SQL for the chosen database
# (Postgres 9.5 and later)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# Validation or export of schema DDL to the database (create, create-drop, validate, update, none)
spring.jpa.hibernate.ddl-auto=update
# Logging JPA Queries
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Spring Security
# HTTP authentication credentials
spring.security.user.name=admin
spring.security.user.password=admin